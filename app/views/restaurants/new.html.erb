

<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, collection: Restaurant::TYPE %>
  <%= f.button :submit, 'Create', class: 'btn-primary' %>
<% end %>
